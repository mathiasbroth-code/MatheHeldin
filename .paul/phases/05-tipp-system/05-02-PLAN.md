---
phase: 05-tipp-system
plan: 02
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - src/stages/mengen20/meta.ts
  - src/stages/buendeln/meta.ts
  - src/stages/bis100lesen/meta.ts
  - src/stages/bis100legen/meta.ts
  - src/stages/bis1000lesen/meta.ts
  - src/stages/bis1000legen/meta.ts
  - src/stages/bis10000lesen/meta.ts
  - src/stages/bis10000legen/meta.ts
  - src/stages/zerlegen/meta.ts
  - src/stages/skizze/meta.ts
autonomous: false
---

<objective>
## Goal
Add erklaerung + tipps content to all 10 remaining stages. After this, every stage in the app has 3-level tips and "Was lernst du hier?" explanations.

## Purpose
Complete the pedagogical scaffolding for all exercises. Every child gets help when stuck, and can understand what they're learning before they start.
</objective>

<context>
@.paul/phases/05-tipp-system/05-01-SUMMARY.md — Infrastructure ready, Stage<T> has optional erklaerung + tipps
@src/stages/mitte/meta.ts — Pattern reference for tipps/erklaerung content
</context>

<acceptance_criteria>

## AC-1: All 10 stages have erklaerung
```gherkin
Given any stage is opened
When user taps "?"
Then "Was lernst du hier?" + "So geht's" text appears
And the content is specific to that exercise type
```

## AC-2: All 10 stages have 3-level tipps
```gherkin
Given any stage is opened
When user taps "Tipp" progressively
Then 3 levels appear with task-specific hints
And level 3 gives the complete solution path
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add erklaerung + tipps to all 10 stages</name>
  <files>All 10 meta.ts files listed in files_modified</files>
  <action>
    Add `erklaerung` and `tipps` to each stage's meta.ts, following the mitte pattern.
    Import `fmt` and `digits` from helpers as needed.
    
    Content guidelines (Prof. Sommer):
    - wasLernstDu: 1-2 sentences, child-friendly, what skill this trains
    - soGehts: concrete method description, how to approach
    - Tipp 1 (Denkanstoß): question that guides thinking, no answer
    - Tipp 2 (Methode): describe the approach/method
    - Tipp 3 (Schritt-für-Schritt): concrete calculation with actual numbers from aufgabe
    - All in German, simple language for 9-year-olds
    - Use fmt() for numbers in tips
  </action>
  <verify>
    - pnpm tsc --noEmit passes
    - pnpm build succeeds
  </verify>
  <done>AC-1, AC-2 satisfied</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Tipps + Erklärungen für alle 11 Stages</what-built>
  <how-to-verify>
    1. pnpm dev → Stichproben: mengen20, buendeln, bis1000legen, zerlegen, skizze
    2. Jede Stage: "?" → Erklärung prüfen (kindgerecht? verständlich?)
    3. Jede Stage: Tipp 1→2→3 durchklicken
    4. Level 3 enthält konkrete Zahlen aus der aktuellen Aufgabe
    5. Alle 11 Stages haben Tipp-Button + "?"
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- Stage Views (nur meta.ts Dateien)
- src/components/*, src/hooks/*, src/db/*
- src/pages/*
</boundaries>

<verification>
- [ ] pnpm tsc --noEmit
- [ ] pnpm build
- [ ] All 11 stages show "?" + Tipp
</verification>

<success_criteria>
- All 11 stages have erklaerung + tipps
- Phase 5 complete
</success_criteria>

<output>
After completion, create `.paul/phases/05-tipp-system/05-02-SUMMARY.md`
</output>
