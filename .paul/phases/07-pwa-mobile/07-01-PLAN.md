---
phase: 07-pwa-mobile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - vite.config.ts
  - src/styles/globals.css
  - src/main.tsx
autonomous: false
---

<objective>
## Goal
Polish the PWA for iPad/iPhone installation: consistent theme color, offline fallback, iOS splash screen meta tags, touch optimization, and install prompt handling.

## Purpose
The app must feel native on Philippa's iPad. "Zum Homebildschirm hinzufügen" should produce a proper app icon, splash screen, and offline-capable standalone experience.
</objective>

<context>
@index.html — has viewport-fit=cover, apple-touch-icon, but theme-color mismatch
@vite.config.ts — VitePWA configured with manifest + icons
@src/styles/globals.css — has safe-area classes, reduced-motion, tap-highlight
@src/components/layout/TabBar.tsx — has safe-area-bottom
</context>

<acceptance_criteria>

## AC-1: Consistent theme color and meta tags
```gherkin
Given the app loads
Then theme-color in HTML and manifest match (#0d9488)
And apple-mobile-web-app-capable is set
And apple-mobile-web-app-status-bar-style is "black-translucent"
```

## AC-2: Offline works
```gherkin
Given the app is installed as PWA
When the device goes offline
Then cached pages and assets still load
And exercises with generated tasks work (no network needed)
```

## AC-3: iOS-specific optimizations
```gherkin
Given the app runs on iOS Safari/standalone
Then viewport-fit=cover prevents notch overlap
And safe-area insets are respected (header, tabbar)
And there is no rubber-band bounce on the main scroll area
And text selection is disabled on UI elements (not inputs)
```

## AC-4: Touch optimization verified
```gherkin
Given the app runs on a touch device
Then all interactive elements are ≥ 44px
And no 300ms tap delay (touch-action: manipulation)
And no tap highlight (already set)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: PWA polish (meta tags, theme, offline, iOS)</name>
  <files>index.html, vite.config.ts, src/styles/globals.css</files>
  <action>
    **index.html:**
    - Fix theme-color: #10b981 → #0d9488 (match manifest)
    - Change apple-mobile-web-app-status-bar-style to "black-translucent" (for notch)
    - Add apple-mobile-web-app-title: "Mathe-Heldin"

    **vite.config.ts:**
    - Add workbox runtimeCaching for offline support:
      ```ts
      workbox: {
        globPatterns: ['**/*.{js,css,html,ico,png,svg,woff2}'],
      }
      ```
    - This ensures all assets are precached by the service worker

    **src/styles/globals.css:**
    - Add overscroll-behavior: none on html (prevent pull-to-refresh in standalone)
    - Add user-select: none on buttons/interactive elements (not inputs)
    - Verify touch-action: manipulation is on body (already present)
  </action>
  <verify>
    - pnpm tsc --noEmit
    - pnpm build — check SW precaches all assets
    - grep theme-color index.html — shows #0d9488
  </verify>
  <done>AC-1, AC-2, AC-3, AC-4 satisfied</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>PWA-Optimierungen für iPad/iPhone</what-built>
  <how-to-verify>
    1. pnpm build && pnpm preview → http://localhost:8009
    2. Chrome DevTools → Application → Manifest prüfen (Icons, theme_color)
    3. Application → Service Workers → Status "activated"
    4. Network → Offline schalten → Seite neu laden → funktioniert
    5. Lighthouse → PWA Audit durchführen (optional)
    6. Auf iPad/iPhone: "Zum Homebildschirm" → App-Icon korrekt
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- src/stages/*, src/components/*, src/pages/* (kein Feature-Code)
- src/db/* (kein Schema-Change)

## SCOPE LIMITS
- Kein Push-Notification
- Kein Background-Sync
- Kein Custom Install-Prompt UI (Browser-default reicht)
</boundaries>

<verification>
- [ ] pnpm build succeeds
- [ ] Service Worker precaches all assets
- [ ] theme-color consistent
- [ ] Offline works after first load
</verification>

<success_criteria>
- PWA installierbar mit korrektem Icon
- Offline-fähig nach erstem Laden
- iOS-optimiert (safe-area, no bounce, no highlight)
- Phase 7 + Milestone v0.1 complete
</success_criteria>

<output>
After completion, create `.paul/phases/07-pwa-mobile/07-01-SUMMARY.md`
</output>
