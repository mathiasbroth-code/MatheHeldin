---
phase: 18-daten-visualisierungen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/daten/BalkenDiagramm.tsx
  - src/components/daten/StrichListe.tsx
  - src/aufgaben/views/AufgabeWrapper.tsx
autonomous: false
---

<objective>
## Goal
Zwei Daten-Visualisierungen: BalkenDiagramm für tabellen-diagramme und StrichListe für haeufigkeitsanalyse Aufgaben.

## Purpose
7 tabellen-diagramme Aufgaben beschreiben Balkendiagramme im Text — Kinder sollen Werte "ablesen" ohne ein Diagramm zu sehen. 29 haeufigkeitsanalyse Aufgaben zählen Buchstaben — eine Strichliste hilft beim Zählen.

## Output
- BalkenDiagramm.tsx: SVG-Balkendiagramm mit Labels und Werten
- StrichListe.tsx: Strichlisten-Darstellung (5er-Bündel)
- AufgabeWrapper-Integration
</objective>

<context>
## Betroffene Stages
- tabellen-diagramme: 7 Aufgaben — Text beschreibt Diagramme/Tabellen
- haeufigkeitsanalyse: 29 Aufgaben — Buchstaben in Texten zählen

## Visualisierungs-Ansatz
- BalkenDiagramm: Horizontale Balken mit Beschriftung, auto-Skalierung
- StrichListe: Klassische 5er-Bündel (IIII mit Querstrich), Alphabet-Zeile
</context>

<acceptance_criteria>

## AC-1: BalkenDiagramm bei tabellen-diagramme
```gherkin
Given eine Aufgabe mit stageId "tabellen-diagramme" die Zahlenwerte enthält
When die Aufgabe angezeigt wird
Then erscheint ein horizontales Balkendiagramm mit den extrahierten Werten
```

## AC-2: StrichListe bei haeufigkeitsanalyse
```gherkin
Given eine Aufgabe mit stageId "haeufigkeitsanalyse"
When die Aufgabe angezeigt wird
Then erscheint eine leere Strichlisten-Vorlage mit Buchstaben A-Z
```

## AC-3: Kaskaden-Integration
```gherkin
Given eine Aufgabe OHNE Daten-Kontext
When angezeigt
Then keine Daten-Visualisierung
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: BalkenDiagramm + StrichListe erstellen</name>
  <files>src/components/daten/BalkenDiagramm.tsx, src/components/daten/StrichListe.tsx</files>
  <action>
    **BalkenDiagramm.tsx:**
    - Props: `{ balken: { label: string; wert: number }[]; einheit?: string }`
    - Horizontale Balken, Breite proportional zum max-Wert
    - Labels links, Werte rechts am Balken
    - Farbe: primary-Palette (teal)
    - Auto-Skalierung auf max-Wert

    **StrichListe.tsx:**
    - Props: `{ buchstaben?: string[] }` — welche Buchstaben als Zeilen anzeigen
    - Default: Vokale (A, E, I, O, U) — häufigste Aufgabenform
    - Zeigt leere Strichlisten-Zeilen als visuelle Hilfe
    - Klassisches 5er-Bündel-Symbol als Beispiel in der ersten Zeile
  </action>
  <verify>pnpm tsc --noEmit</verify>
  <done>AC-1 + AC-2 Komponenten erstellt</done>
</task>

<task type="auto">
  <name>Task 2: AufgabeWrapper-Kaskade erweitern</name>
  <files>src/aufgaben/views/AufgabeWrapper.tsx</files>
  <action>
    **Parser `parseDatenViz(stageId, text)`:**
    - tabellen-diagramme: Extrahiere Tabellenwerte (Name|Wert Paare aus Markdown-Tabellen)
    - haeufigkeitsanalyse: Erkenne welche Buchstaben gezählt werden sollen

    **Kaskade:** Nach geoDaten, vor kreiseDaten
  </action>
  <verify>pnpm build — Erfolg</verify>
  <done>AC-3 satisfied</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>BalkenDiagramm + StrichListe für Daten-Aufgaben</what-built>
  <how-to-verify>
    1. pnpm dev
    2. Kapitel 13 → "Tabellen und Diagramme" → Balkendiagramm sichtbar?
    3. Kapitel 7 → "Buchstaben zählen" → Strichlisten-Vorlage sichtbar?
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- Bestehende Visualisierungen, Aufgaben-JSON
## SCOPE LIMITS
- Nicht interaktiv (keine Klick-Strichliste)
- BalkenDiagramm nur bei tabellen-diagramme, nicht universell
</boundaries>

<verification>
- [ ] pnpm build → 0 Fehler
- [ ] BalkenDiagramm bei tabellen-diagramme Aufgaben
- [ ] StrichListe bei haeufigkeitsanalyse Aufgaben
- [ ] Keine Regression
</verification>

<success_criteria>
- 2 Daten-Viz-Komponenten, Build grün
</success_criteria>

<output>
After completion, create `.paul/phases/18-daten-visualisierungen/18-01-SUMMARY.md`
</output>
